__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
.env
outputs/plots/
.vscode/
# Keep trained models but exclude large datasets if data dir gets heavy.
# For this project, we want data/ so stream_simulator works, so we don't ignore all of data/
frontend/
node_modules/
.dockerignore
Dockerfile
docker-compose.yml
